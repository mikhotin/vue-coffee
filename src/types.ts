export type Coffee = {
  id: number;
  uid: string;
  blend_name: string;
  intensifier: string;
  notes: string;
  origin: string;
  variety: number;
};

export type Image = {
  id: string;
  url: string;
};
